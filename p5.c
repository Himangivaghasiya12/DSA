#include<stdio.h>
int main()
{
    //int a=1 , b=2, c;
    //c = a && b; 121
    //printf("a=%d\nb=%d\nc=%d",a,b,c);
    //c = a && b; 121
    //printf("a=%d\nb=%d\nc=%d",a,b,c);
    //c = a && ++b; 131
    //printf("a=%d\nb=%d\nc=%d",a,b,c);
    //c = ++a && b; 221
    //printf("a=%d\nb=%d\nc=%d",a,b,c);
    //c = --a && b; 020
    //printf("a=%d\nb=%d\nc=%d",a,b,c);
    //c = --a && ++b; 020
    //printf("a=%d\nb=%d\nc=%d",a,b,c);
    //c = --a && --b; 020
    //printf("a=%d\nb=%d\nc=%d",a,b,c);
    //c = --a && ++b; 020
    //printf("a=%d\nb=%d\nc=%d",a,b,c);
    
    //int a= 1,b=2 , c;
    //c = a || b; 121
    //printf("a=%d\nb=%d\nc=%d",a,b,c);
    //c = a || b; 121
    //printf("a=%d\nb=%d\nc=%d",a,b,c);
    //c = a || ++b; 131
    //printf("a=%d\nb=%d\nc=%d",a,b,c);
    //c = ++a || b; 221
    //printf("a=%d\nb=%d\nc=%d",a,b,c);
    //c = --a || b; 020
    //printf("a=%d\nb=%d\nc=%d",a,b,c);
    //c = --a || ++b; 020
    //printf("a=%d\nb=%d\nc=%d",a,b,c);
    //c = --a || --b; 020
    //printf("a=%d\nb=%d\nc=%d",a,b,c);
    //c = --a || ++b; 020
    //printf("a=%d\nb=%d\nc=%d",a,b,c);

    int a=1 , b = 2, c;
    c = a || b;
    printf(" a= %d , b = %d, c=%d",a,b,c);
    
}